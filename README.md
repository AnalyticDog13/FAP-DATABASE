# FAP-DATABASE
A self-updating and queryable database (using FastAPI) for non-profit hospital FAP data. Utilizes alembic migration, Claude API calls, and scrapes FAP documents automatically on schedule.
